// This is a single-line comment
int main() {
    int speed = 70;
    double ratio = .5;
    /* This is a
                          multi-line comment */

    char letter = 'A';
    char* msg = "Hello, AtomC!";

    if (speed >= 60 && ratio != 0) {
        speed = 0xAFd9; // Hexadecimal test
        ratio = 017;    // Octal test
    }

    x= 0x711
    return 0;
}